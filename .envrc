# Loaded by direnv when entering this repo.
# Exports WORKSHOP_DIR; the ~/.zshrc hook then sources zarf/shell/workshop.zsh.
export WORKSHOP_DIR="$PWD"
watch_file zarf/shell/workshop.zsh

[ -f ".env" ] && . .env
